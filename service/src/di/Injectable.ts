export function Injectable() {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  return function (target: object) {
    // do something with `target`, for example decorate it with some metadata
  }
}
